{
  "name": "bsg",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "test": "tsx server/src/test-ships.ts && tsx server/src/test-missions.ts && tsx server/src/test-events.ts && tsx server/src/test-personnel.ts && tsx server/src/test-scenario.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.5.0",
    "typescript": "^5.7.0"
  }
}
